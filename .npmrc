# npm registry — automation token injected by CI via NODE_AUTH_TOKEN env var
# For local publishing, set NPM_TOKEN in ~/.npmrc instead:
#   //registry.npmjs.org/:_authToken=npm_YOUR_TOKEN_HERE
access=public
legacy-peer-deps=true
